name: Documentation Check

on:
  push:
    branches: [main, develop]
    paths:
      - "**.md"
      - "scripts/validate-docs.ts"
      - "packages/cli/test/**"
  pull_request:
    branches: [main, develop]
    paths:
      - "**.md"
      - "scripts/validate-docs.ts"
      - "packages/cli/test/**"

jobs:
  validate-docs:
    name: Validate Documentation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build scripts
        run: npm run build

      - name: Run full validation (structure + docs)
        run: npm run validate
        id: validate
        continue-on-error: false

      - name: Run documentation validation
        run: npm run docs:check
        id: validate-docs
        continue-on-error: false

      - name: Run tests
        run: npm run test
        continue-on-error: false

      - name: Check for broken links
        uses: lycheeverse/lychee-action@v1
        with:
          args: --timeout 30 --max-retries 3 --exclude-mail "**/*.md"
          fail: false
        id: lychee

      - name: Report status
        if: always()
        run: |
          echo "## Documentation Validation Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.validate.outcome }}" == "success" ]; then
            echo "[PASS] Documentation validation passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "[FAIL] Documentation validation failed" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "${{ steps.lychee.outcome }}" == "success" ]; then
            echo "[PASS] Link check passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "[WARN] Link check completed with issues" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docs-validation-report
          path: |
            docs-report.json
            lychee/out.md
          retention-days: 7
